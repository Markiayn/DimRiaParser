# DimRiaParser - Парсер нерухомості Dom.ria

Сучасний Java-парсер для збору даних про нерухомість з сайту dom.ria.com з покращеною архітектурою та функціональністю.

## 🏗 Архітектура проекту

```
src/main/java/org/example/
├── config/
│   └── AppConfig.java          # Централізована конфігурація
├── database/
│   └── DatabaseManager.java    # Робота з базою даних (Singleton)
├── model/
│   └── Apartment.java          # Модель даних квартири
├── service/
│   └── RiaParserService.java   # Основний сервіс парсингу
├── utils/
│   └── FileUtils.java          # Утиліти для роботи з файлами
└── Main.java                   # Точка входу в додаток
```

## ✨ Основні покращення

### 1. **Модульна архітектура**
- Розділення відповідальності між класами
- Використання паттерну Singleton для DatabaseManager
- Централізована конфігурація через AppConfig

### 2. **Покращена обробка помилок**
- Try-catch блоки з детальним логуванням
- Graceful handling помилок мережі та бази даних
- Інформативні повідомлення про помилки

### 3. **Конфігурація через файл**
- `config.properties` для налаштувань
- Легка зміна параметрів без перекомпіляції
- Значення за замовчуванням

### 4. **Покращена робота з файлами**
- Безпечне видалення файлів
- Перевірка існування папок
- Підрахунок розміру та кількості файлів

### 5. **Модель даних**
- Клас `Apartment` з усіма необхідними полями
- Типізовані дані замість примітивів
- Методи для роботи з фотографіями

## 🚀 Швидкий старт

### Вимоги
- Java 11+
- Maven
- Chrome/Chromium браузер
- ChromeDriver

### Встановлення

1. **Клонуйте репозиторій:**
```bash
git clone <repository-url>
cd DimRiaParser
```

2. **Налаштуйте конфігурацію:**
Відредагуйте `config.properties`:
```properties
# Chrome Driver налаштування
chrome.driver.path=src/main/java/org/example/chromedriver-win64/chromedriver.exe

# База даних
database.url=jdbc:sqlite:test.db

# Фотографії
photos.directory=photos
max.photos.per.apartment=5

# Параметри парсингу
hours.limit=48
max.pages=2
min.rooms=1
min.area=25.0
verbose=true
```

3. **Запустіть парсер:**
```bash
mvn compile exec:java -Dexec.mainClass="org.example.Main"
```

## 📊 Функціональність

### Парсинг даних
- ✅ Збір оголошень з dom.ria.com
- ✅ Фільтрація за датою публікації
- ✅ Фільтрація за кількістю кімнат та площею
- ✅ Завантаження фотографій
- ✅ Отримання контактних даних

### Зберігання даних
- ✅ SQLite база даних
- ✅ Розділення по регіонах (Львів, Івано-Франківськ)
- ✅ Відстеження статусу публікації
- ✅ Збереження метаданих

### Додаткові можливості
- ✅ Telegram бот інтеграція (готово до використання)
- ✅ Детальна статистика парсингу
- ✅ Логування процесу
- ✅ Обробка помилок

## 🔧 Налаштування

### Параметри парсингу

| Параметр | Опис | За замовчуванням |
|----------|------|------------------|
| `hours.limit` | Ліміт годин для оголошень | 48 |
| `max.pages` | Максимальна кількість сторінок | 2 |
| `min.rooms` | Мінімальна кількість кімнат | 1 |
| `min.area` | Мінімальна площа (м²) | 25.0 |
| `max.photos.per.apartment` | Максимум фото на квартиру | 5 |

### Регіони

| ID | Регіон |
|----|--------|
| 5 | Львівська область |
| 15 | Івано-Франківська область |

### Типи нерухомості

| ID | Тип |
|----|-----|
| 2 | Квартира |
| 1 | Будинок |
| 3 | Кімната |

### Типи операцій

| ID | Операція |
|----|----------|
| 3 | Оренда |
| 1 | Продаж |

## 📈 Статистика

Парсер надає детальну статистику:
- Кількість знайдених оголошень
- Кількість оброблених квартир
- Кількість відфільтрованих оголошень
- Розмір завантажених фотографій

## 🛠 Розробка

### Додавання нових регіонів

1. Додайте новий виклик в `Main.java`:
```java
parserService.parseApartments(
    "Apartments_NewRegion",
    regionId,    // ID регіону
    null,        // ID міста (null для всієї області)
    2,           // тип нерухомості
    3,           // тип операції
    // ... інші параметри
);
```

2. Створіть таблицю в базі даних (автоматично)

### Розширення моделі даних

1. Додайте нові поля в `Apartment.java`
2. Оновіть `DatabaseManager.java` для роботи з новими полями
3. Модифікуйте `RiaParserService.java` для парсингу нових даних

## 🔒 Безпека

- Використання PreparedStatement для SQL запитів
- Валідація вхідних даних
- Безпечна робота з файловою системою
- Обробка мережевих помилок

## 📝 Логування

Парсер використовує детальне логування:
- ✅ Успішні операції
- ⚠️ Попередження
- ❌ Помилки
- 📊 Статистика

## 🤝 Внесок

1. Fork репозиторію
2. Створіть feature branch
3. Зробіть commit змін
4. Push до branch
5. Створіть Pull Request

## 📄 Ліцензія

Цей проект розповсюджується під ліцензією MIT.

## 🆘 Підтримка

Якщо у вас виникли питання або проблеми:
1. Перевірте налаштування в `config.properties`
2. Переконайтеся, що ChromeDriver встановлений
3. Створіть Issue в репозиторії

---

**Автор:** Розроблено для ефективного парсингу нерухомості з dom.ria.com 